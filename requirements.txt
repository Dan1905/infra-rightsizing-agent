# LLM -- the agent talks to whichever provider LLM_PROVIDER selects
openai>=1.40.0        # groq / xAI / any OpenAI-compatible endpoint (default)
anthropic>=1.0.0      # only needed for LLM_PROVIDER=anthropic

# Config
python-dotenv>=1.0.0

# Metrics + execution
requests>=2.31.0
docker>=7.1.0

# RAG
chromadb>=0.5.0
sentence-transformers>=3.0.0
